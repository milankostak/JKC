{var $title = "Smazat článek: ".$article->title}

{block content}

{include article_menu.latte}

<div id="article_content">
	<h2 class="bigger">Smazat článek: {$article->title}</h2>

	{include one_article_menu.latte}

	<p>Opravdu chcete <span n:if="$article->draft == 1">rozepsaný</span> článek <em>{$article->title}</em>
		<span n:if="$article->draft == 0">včetně jeho komentářů</span> <strong>nenávratně</strong> smazat?</p>
	{control deleteArticleForm}
</div>
<div class="clear"></div>
